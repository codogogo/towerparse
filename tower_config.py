# You can edit these config values
device = "cuda:0" # where to run the parser, on GPU ("cuda:X") or CPU ("cpu")

max_length = 256 # maximal input length in terms of XLM-R Base's subword tokens (max is 512)
max_word_len = 140 # maximal length of the sentences to be parse in terms of word-level tokens







